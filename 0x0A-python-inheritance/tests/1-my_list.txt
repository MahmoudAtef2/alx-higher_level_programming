===========================
tests for 1-mylist.py
===========================

Import function:
    >>> MyList = __import__('1-my_list').MyList

Correct Instance test:
    >>> mylist = MyList()
    >>> isinstance(mylist, list)
    True

Test that MyList inherits from list:
    >>> issubclass(MyList, list)
    True

Test with empty list:
    >>> mylist = MyList()
    >>> mylist.print_sorted()
    []

Test that append work:
    >>> mylist.append(1)
    >>> mylist.append(4)
    >>> mylist.append(2)
    >>> mylist.append(3)
    >>> mylist.append(5)
    >>> print(mylist)
    [1, 2, 3, 4, 5]

Test that print_sorted() work:
    >>> mylist.append(3)
    >>> mylist.append(5)
    >>> mylist.append(1)
    >>> mylist.append(4)
    >>> mylist.append(2)
    >>> print(mylist)
    [3, 5, 1, 4, 2]
    >>> mylist.print_sorted()
    [1, 2, 3, 4, 5]
    >>> print(mylist)
    [3, 5, 1, 4, 2]

Test negative numbers:
    >>> mylist = MyList()
    >>> mylist.append(1)
    >>> mylist.append(-4)
    >>> mylist.append(-2)
    >>> mylist.append(-3)
    >>> print(mylist)
    [1, -4, -2, -3]
    >>> mylist.print_sorted()
    [-4, -3, -2, 1]
    >>> print(mylist)
    [1, -4, -2, -3]

Checking for too many arguments:
    >>> mylist.print_sorted(1)
    Traceback (most recent call last):
    ...
    TypeError: print_sorted() takes 1 positional argument but 2 were given
